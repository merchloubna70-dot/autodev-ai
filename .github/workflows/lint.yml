name: Lint

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Setup autodev
        uses: ./.github/actions/setup-autodev
        with:
          python-version: "3.12"
          extra-packages: mypy

      - name: ruff check
        run: ruff check src tests scripts

      - name: mypy (strict — matches release_readiness_gate)
        run: mypy src/autodev

      - name: pytest with coverage
        run: pytest tests/ --cov=src/autodev --cov-report=json --cov-report=term -q

      - name: coverage gate
        run: python scripts/coverage_gate.py --strict
